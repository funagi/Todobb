require(['jquery', 'backbone', 'collections/todos'], function ($, Backbone) {    var app = app || {};    console.log(app)    app.Appview = Backbone.View.extend({        el: '#todoApp',        initialize: function () {            this.$addNew = this.$('#todo-new');        },        events: {            'keydown #todo-new': 'createTodo'        },        createTodo: function (e) {            if (e.which !== 13 || !this.$addNew.val().trim()) return;            app.todoCollection.create(this.$addNew.newTodo());        },        newTodo: function () {            return {                title: this.val(),                completed: false            }        }    });});